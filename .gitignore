**/.terraform/*
*.tfstate
*.tfstate.*
ansible/inventory.ini
*.retry
# NOTE: ansible/group_vars/all.yml is intentionally NOT ignored - it's
# ansible-vault encrypted, so committing it (encrypted) is expected.
# Only ever ignore/never-commit the *decrypted* version if you keep one
# locally under a different filename.
